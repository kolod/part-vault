# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        variant:
          - windows-latest-msvc-release
          - ubuntu-latest-gcc-release
          - ubuntu-latest-clang-release
        include:
          - variant: windows-latest-msvc-release
            os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            qt_arch: "win64_msvc2022_64"
            build_type: Release
          - variant: ubuntu-latest-gcc-release
            os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
            qt_arch: "linux_gcc_64"
            build_type: Release
          - variant: ubuntu-latest-clang-release
            os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++
            qt_arch: "linux_gcc_64"
            build_type: Release

    steps:
    - uses: actions/checkout@v6

    - name: Install Qt
      # Install Qt using the jurplel/install-qt-action. 
      # We are installing the official version of Qt 6.10.0 for desktop development on both Windows and Linux.
      uses: jurplel/install-qt-action@v4
      with:
        version: "6.10.0"
        target: "desktop"
        arch: ${{ matrix.qt_arch }}        
        use-official: true
        email: ${{ secrets.QT_EMAIL }}
        pw: ${{ secrets.QT_PW }}

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -S ${{ github.workspace }}

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }}

  windows-installer:
    runs-on: windows-latest
    needs: build

    steps:
    - uses: actions/checkout@v6

    # ── AMD64 ──────────────────────────────────────────────────────────────────

    - name: Install Qt amd64 + IFW
      uses: jurplel/install-qt-action@v4
      with:
        version: "6.10.0"
        target: "desktop"
        arch: "win64_msvc2022_64"
        use-official: true
        email: ${{ secrets.QT_EMAIL }}
        pw: ${{ secrets.QT_PW }}
        tools: "tools_ifw"

    - name: Set up MSVC (amd64)
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: amd64

    - name: Configure CMake (amd64)
      run: >
        cmake -B ${{ github.workspace }}/build-amd64
        -DCMAKE_BUILD_TYPE=Release
        -DENABLE_TESTS=OFF
        -DENABLE_DUMMY_DATA=OFF
        -DENABLE_IFW_INSTALLER=ON
        -S ${{ github.workspace }}

    - name: Build IFW installer (amd64)
      run: cmake --build ${{ github.workspace }}/build-amd64 --config Release --target ifw_installer

    - name: Rename amd64 installer
      shell: bash
      run: mv "${{ github.workspace }}/build-amd64/PartVault-Installer-"*.exe "${{ github.workspace }}/build-amd64/PartVault-Installer-amd64.exe"

    # ── ARM64 ──────────────────────────────────────────────────────────────────

    - name: Install Qt arm64
      uses: jurplel/install-qt-action@v4
      with:
        version: "6.10.0"
        target: "desktop"
        arch: "win64_msvc2022_arm64"
        use-official: true
        email: ${{ secrets.QT_EMAIL }}
        pw: ${{ secrets.QT_PW }}

    - name: Set up MSVC (amd64_arm64)
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: amd64_arm64

    - name: Configure CMake (arm64)
      run: >
        cmake -B ${{ github.workspace }}/build-arm64
        -DCMAKE_BUILD_TYPE=Release
        -DENABLE_TESTS=OFF
        -DENABLE_DUMMY_DATA=OFF
        -DENABLE_IFW_INSTALLER=ON
        -S ${{ github.workspace }}

    - name: Build IFW installer (arm64)
      run: cmake --build ${{ github.workspace }}/build-arm64 --config Release --target ifw_installer

    - name: Rename arm64 installer
      shell: bash
      run: mv "${{ github.workspace }}/build-arm64/PartVault-Installer-"*.exe "${{ github.workspace }}/build-arm64/PartVault-Installer-arm64.exe"

    # ── Upload both ────────────────────────────────────────────────────────────

    - name: Upload amd64 installer artifacts
      uses: actions/upload-artifact@v4
      with:
        name: partvault-windows-installer-amd64
        path: ${{ github.workspace }}/build-amd64/PartVault-Installer-amd64.exe
        if-no-files-found: error

    - name: Upload arm64 installer artifacts
      uses: actions/upload-artifact@v4
      with:
        name: partvault-windows-installer-arm64
        path: ${{ github.workspace }}/build-arm64/PartVault-Installer-arm64.exe
        if-no-files-found: error

  deb-package:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/checkout@v6

    - name: Install packaging tools
      run: sudo apt-get install -y dpkg-dev

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: "6.10.0"
        target: "desktop"
        arch: "linux_gcc_64"
        use-official: true
        email: ${{ secrets.QT_EMAIL }}
        pw: ${{ secrets.QT_PW }}

    - name: Configure CMake
      run: >
        cmake -B ${{ github.workspace }}/build-deb
        -DCMAKE_CXX_COMPILER=g++
        -DCMAKE_C_COMPILER=gcc
        -DCMAKE_BUILD_TYPE=Release
        -DCMAKE_INSTALL_PREFIX=/usr
        -DENABLE_TESTS=OFF
        -DENABLE_DUMMY_DATA=OFF
        -DENABLE_IFW_INSTALLER=OFF
        -S ${{ github.workspace }}

    - name: Build
      run: cmake --build ${{ github.workspace }}/build-deb --config Release

    - name: Package
      working-directory: ${{ github.workspace }}/build-deb
      run: cpack -G DEB

    - name: Upload .deb artifact
      uses: actions/upload-artifact@v4
      with:
        name: partvault-deb-package
        path: ${{ github.workspace }}/build-deb/*.deb
        if-no-files-found: error
